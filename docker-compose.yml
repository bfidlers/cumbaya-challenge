services:
  trip-service:
    build: ./trip-service
    ports:
      - "8000:8000"

  ai-service:
    build: ./ai-service
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    ports:
      - "8001:8000"